/**
 * Built-in presets — intentionally empty.
 * Add entries here to ship default personalities, situations, or configs.
 */

import type { PersonalityPreset, SituationPreset, PromptConfig } from "./types";

export const BUILT_IN_PERSONALITIES: PersonalityPreset[] = [];

export const BUILT_IN_SITUATIONS: SituationPreset[] = [];

export const BUILT_IN_CONFIGS: PromptConfig[] = [];
